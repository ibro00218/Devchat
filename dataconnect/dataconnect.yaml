specVersion: "v1"
serviceId: "devcodechat"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "devcodechat-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
